mutation AddItem(
    $graphId: ID!
    $name: String!)
{
    addItem(input: 
    {
        graphId: $graphId
        name: $name
    })
    {
        item {
            id
            name
        }
    }
}